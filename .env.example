# =====================================================================
# SETUP: Copy this file to .env.local and fill in your values
#   cp .env.example .env.local
#
# OR pull from Vercel (fastest -- gets all values automatically):
#   npx vercel login
#   npx vercel link   (select "crazygels-theme" project)
#   npx vercel env pull .env.local
# =====================================================================

# ===========================================
# Shopify Storefront (headless)
# ===========================================
SHOPIFY_STORE_DOMAIN=crazygels.myshopify.com
NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN=crazygels.myshopify.com
SHOPIFY_STOREFRONT_ACCESS_TOKEN=           # From Shopify Admin > Settings > Apps > Headless > Storefront API
SHOPIFY_ADMIN_TOKEN=                       # From Shopify Admin > Settings > Apps > Custom App > Admin API

# ===========================================
# Database (Neon)
# ===========================================
DATABASE_URL=                              # From Vercel > Project > Settings > Environment Variables (or Neon dashboard)

# ===========================================
# Admin
# ===========================================
ADMIN_TOKEN=                               # Your chosen admin password for /admin pages

# ===========================================
# Klaviyo (optional -- site works without these)
# ===========================================
NEXT_PUBLIC_KLAVIYO_PUBLIC_KEY=
KLAVIYO_PRIVATE_API_KEY=
KLAVIYO_LIST_ID=
